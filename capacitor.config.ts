import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloeys.whattoeat',
  appName: 'what-to-eat',
  webDir: 'dist/what-to-eat/browser'
};

export default config;
